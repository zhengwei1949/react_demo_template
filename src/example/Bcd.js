import React,{Component} from 'react'
import ReactDOM from 'react-dom'

export default class Bcd extends Component{
    render(){
        return (
            <div>
                <p>{this.props.m}</p>
            </div>
        )
    }
}
